### P7 — Hardening + launch (M)
**Depends**: all.
**Deliverables**: security headers middleware (CSP for blog pages: `default-src 'none'; img-src * data:; style-src 'unsafe-inline'; media-src *`, X-Frame-Options on apex, Referrer-Policy), abuse blocklist admin (env-var allowlisted admin pubkey; block user → 404s blog + drops from discover), final rate-limit review, WAF setup doc (one zone rate rule + `.php/.env` block — dashboard config, documented in `docs/ops.md`), deploy gates runbook: Gate A `wrangler deploy` to workers.dev preview route (CI+smoke green) → Gate B DNS (apex + proxied `*` wildcard CNAME) + routes live + smoke vs prod + cron observed in logs.
**Tests**: header assertions per host class; blocklist integration; full smoke (~12 curls) vs dev and preview.
**Accept**: prod live at nbread.lol with a claimed test blog rendering real relay content.
**Deferred backlog**: custom domains (CF for SaaS), comments (Nostr replies), zaps/reactions counts, R2 media proxy, NIP-46 remote-signer login.

