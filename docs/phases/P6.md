### P6 — Discover + search (S)
**Depends**: P3.
**Deliverables**: `nbread.lol/discover` (recent posts by claimed, non-blocked users, paginated), FTS5 search page (`services/search.ts` with MATCH input sanitizer — quote terms, strip FTS operators), landing page polish (product pitch + login CTA).
**Tests**: feed ordering deterministic (created_at desc, id tie-break); blocked users excluded; search returns expected sets for canned queries over fixtures; MATCH injection strings (`"`, `*`, `NEAR(`, `-`) return safe results not errors.
**Accept**: snapshots + set assertions green.

